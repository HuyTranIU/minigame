class UserService {
    static userAnswer = async ({name, answer}) => {
        try {
            
        } catch (error) {
            return {
                message: error
            }
        }
    }
}